# GitHub 仓库配置
DEFAULT_REPO_URL=git@github.com:your-username/your-repo.git
GITHUB_USERNAME=ghcpd
GITHUB_TOKEN=your_github_token_here

# 模板文件夹名称
MAIN_FOLDER_NAME=main

# 要创建的自定义文件夹名称列表（用逗号分隔，也会作为分支名和PR标题）
CUSTOM_FOLDERS=oswe-mini-m23a1s255-new,oswe-mini-secondary,oswe-mini-m23a3s430,Claude-haiku-4.5,oswe-mini-prime-new,grok-fast

# 排除的文件/文件夹名称（可选，用逗号分隔。如果不配置则上传所有文件，仅排除 .git 文件夹）
EXCLUDE_NAMES=.git,__pycache__,.DS_Store,.venv,venv,env,.env

# PR 描述配置（可选，支持以下方式，优先级从高到低）
# 方式1：从文件读取PR描述（推荐，每个文件夹可以有不同的描述）
#        注意：如果配置了此项，则必须在每个文件夹中创建该文件，否则会跳过该文件夹
PR_DESCRIPTION_FILE=final_prompt.txt

# 方式2：直接配置PR描述内容（适合所有PR使用相同描述的场景）
#        注意：只有未配置 PR_DESCRIPTION_FILE 时，此配置才会生效
# PR_DESCRIPTION=这是PR的描述内容

# 方式3：如果以上都未配置，将使用自动生成的描述：Auto-generated PR for branch: {分支名}


# ============================
# 产物收集（collect_artifacts）配置
# ============================

# 日志级别（可选）：DEBUG / INFO / WARNING / ERROR
# 建议排查“为什么没有生成对应的 txt / time.txt 为空”等问题时设置为 DEBUG
COLLECT_ARTIFACTS_LOG=INFO

# VS Code workspaceStorage 位置选择：
# 同一台机器上可能同时安装 Code 与 Code - Insiders；同一个 repo 下不同 model 文件夹可能分别用不同的 VS Code 打开。
# collect_artifacts 需要知道每个 model 文件夹是用哪个 VS Code 变体打开的，才能去正确的 workspaceStorage 抓聊天记录。

# 可选：默认 VS Code 变体（不指定时默认 Code）
VSCODE_VARIANT_DEFAULT=Code

# 可选：显式声明哪些 model 文件夹是用 Code - Insiders 打开的（大小写不敏感，逗号分隔）
# 例如：CODE_INSIDERS=oswe-mini-m23a3s430,grok-fast
CODE_INSIDERS=